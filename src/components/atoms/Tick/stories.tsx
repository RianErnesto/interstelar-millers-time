import { Meta, StoryObj } from '@storybook/react'
import Tick from '.'

const meta: Meta = {
  title: 'Atoms/Tick',
  component: Tick,
  tags: ['autodocs'],
}

export default meta
export const Default: StoryObj = {}
