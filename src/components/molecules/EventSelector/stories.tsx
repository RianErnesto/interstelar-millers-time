import EventSelector from '.'
import { Meta, StoryObj } from '@storybook/react'

const meta: Meta = {
  title: 'Components/Molecules/EventSelector',
  component: EventSelector,
}

export default meta
export const Default: StoryObj = {}
