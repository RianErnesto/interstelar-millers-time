import { Meta, StoryObj } from '@storybook/react'
import TimeView from '.'

const meta: Meta = {
  title: 'components/Molecules/TimeView',
  component: TimeView,
}

export default meta
export const Default: StoryObj = {}
