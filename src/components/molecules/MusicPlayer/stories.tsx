import { Meta, StoryObj } from '@storybook/react'
import MusicPlayer from '.'

const meta: Meta = {
  title: 'Components/Molecules/MusicPlayer',
  component: MusicPlayer,
}

export default meta

export const Default: StoryObj = {}
